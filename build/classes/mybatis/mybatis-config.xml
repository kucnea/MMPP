<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
  <properties resource="mybatis/db.properties"/>

  <settings> 
  <setting name="jdbcTypeForNull" value="NULL" /> 
  </settings>
    	
  <!--  db 연결정보 -->
  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/>
      <!-- 데이터베이스 연결 풀 설정 -->
      <dataSource type="POOLED">
        <property name="driver" value="${driver}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${username}"/>
        <property name="password" value="${password}"/>
      </dataSource>
    </environment>
  </environments>
  <!-- JDBC로 처리하는 상당부분의 코드와 파라미터 설정 및 결과 매핑 해준다.   -->
  <mappers>
    <mapper resource="mybatis/UserData.xml"/>
    <mapper resource="mybatis/FreeboardData.xml"/>
    <mapper resource="mybatis/CommentData.xml"/>
    <mapper resource="mybatis/ImgboardData.xml"/>
    <mapper resource="mybatis/BuyBasketData.xml"/>
   <!--  <mapper resource="it/mybatis/student.xml"/> -->
  </mappers>
  

  
  
</configuration>


